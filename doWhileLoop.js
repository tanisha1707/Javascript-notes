let index = 0 
while(index <= 10){
    console.log(`value of index is ${index}`);
    index = index + 2
}
let myArray = ['flash', 'batman','spiderman']

let arr=0
while (arr < myArray.length) {
    console.log(`values of ${myArray[arr]}`);
    arr = arr + 1
}

//kam pehle check badme 
let score = 11
do {
    console.log(`score is ${score}`);
    score++
} while (score <= 10);

// advance loops and filter reduce skipped